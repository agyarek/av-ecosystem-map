PORT=3000
NODE_ENV=production
ADMIN_USERNAME=kofi
ADMIN_PASSWORD_HASH=           # generate with: node scripts/set-password.js yourPassword
SESSION_SECRET=                # generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DATABASE_PATH=./data/site.db
